import styled from 'styled-components';

import { SectionPanel } from './SectionPanel';

export const FrontpagePanel = styled(SectionPanel)`
    max-width: 58.25rem;
    padding: 2rem 2rem 1rem 2rem !important;
`;
